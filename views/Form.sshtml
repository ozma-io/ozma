@Master['Master']

@Section['Title']
    Hello! @Model.Titles[0].Name
@EndSection

@Section['Content']
    <div class="main">
        @Partial['Menubar', Model.MenuBar]
        <div class="form_data">
		@Each.Entries1
		xxxx
            @Partial['FormField', Current]
        @EndEach
		</div>
		<!--<div class="form_data">
		@Each.Entries2
		xxxx
            @Partial['FormField', Current]
        @EndEach
		</div>
		-->
	</div>
@EndSection
