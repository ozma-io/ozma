@Master['Master']

@Section['Title']
    Hello! @Model.Titles[0].Name
@EndSection

@Section['Content']
    <div class="main">
        @Partial['Menubar', Model.MenuBar]
        <div class="form_data">
		@Each.Entries
            @Partial['FormField', Current]
        @EndEach
		</div>
		
	</div>
@EndSection
