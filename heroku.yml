setup:
  addons:
    - plan: heroku-postgresql
    - plan: heroku-redis
build:
  docker:
    web: docker/Dockerfile.heroku
release:
  command:
    - echo "Running release phase"
run:
  web: /usr/sbin/caddy run --config /etc/caddy/Caddyfile 