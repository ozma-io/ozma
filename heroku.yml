setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS

build:
  docker:
    web: docker/Dockerfile.heroku

run:
  web: caddy run --config /etc/caddy/Caddyfile 